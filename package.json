{
  "name": "line-bot-nodered",
  "version": "1.0.0",
  "description": "LINE bot with Node-RED on Render",
  "scripts": {
    "start": "node-red -v --userDir /opt/render/project/src --settings ./settings.js ./flows.json"
  }
  "dependencies": {
    "node-red": "^3.1.0"
  }
}
